'use strict';

const assert = require('chai').assert;
const endpointBuilder = require('../lib/validator');

const sinon = require('sinon');

describe('validator', () => {
    it('should validate', () => {});
});
